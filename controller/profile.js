exports.getProfile = async (req, res, next) => {
    try {
        // 处理请求 
        // 发送响应
        res.end('/:username')
    } catch (error) {
        next(error)
    }
}
exports.getFollowUser = async (req, res, next) => {
    try {
        // 处理请求 
        // 发送响应
        res.end('/:username')
    } catch (error) {
        next(error)
    }
}
exports.setUnFollowUser = async (req, res, next) => {
    try {
        // 处理请求 
        // 发送响应
        res.end('/:username')
    } catch (error) {
        next(error)
    }
}
